# 🚀 Grafana Шпаргалка

## 📌 Быстрый старт

```bash
# Запуск системы логирования
cd infra
docker compose -f docker-compose.logging.yml up -d

# Запуск микросервисов
docker compose -f docker-compose.dev.yml up -d

# Открыть Grafana
# http://localhost:3001
# Логин: admin
# Пароль: admin
```

---

## 🔍 LogQL Шпаргалка

### Базовые запросы

```logql
# Все логи сервиса
{service="app-dev"}

# Все микросервисы
{service=~".*-app-dev"}

# Конкретный контейнер
{container="gateway-app-dev"}

# Несколько меток
{service="app-dev", level="error"}
```

### Фильтры содержимого

```logql
# Содержит
{service="app-dev"} |= "error"

# НЕ содержит
{service="app-dev"} != "debug"

# Regex
{service="app-dev"} |~ "user.*created"

# Regex НЕ содержит
{service="app-dev"} !~ "debug|trace"

# Case-insensitive
{service="app-dev"} |~ "(?i)error"
```

### Парсинг JSON

```logql
# Парсинг JSON
{service="app-dev"} | json

# Фильтр по полю JSON
{service="app-dev"} | json | level="error"

# Несколько полей
{service="app-dev"} | json | level="error" | userId="123"
```

### Парсинг с regex

```logql
# Извлечение поля
{service="app-dev"} | regexp "userId=(?P<userId>\\d+)"

# Фильтр по извлеченному полю
{service="app-dev"} | regexp "userId=(?P<userId>\\d+)" | userId="123"
```

### Агрегация

```logql
# Количество логов
count_over_time({service="app-dev"} [5m])

# Сумма по сервисам
sum(count_over_time({service=~".*-app-dev"} [5m])) by (service)

# Среднее
avg(count_over_time({service=~".*-app-dev"} [5m]))

# Максимум
max(count_over_time({service=~".*-app-dev"} [5m]))

# Топ 10
topk(10, sum(count_over_time({service=~".*-app-dev"} [5m])) by (service))

# Процентиль 95
quantile_over_time(0.95, {service="app-dev"} | json | unwrap duration [5m])
```

### Rate (скорость)

```logql
# Логов в секунду
rate({service="app-dev"} [1m])

# Сумма по сервисам
sum(rate({service=~".*-app-dev"} [1m])) by (service)
```

### Unwrap (извлечение чисел)

```logql
# Среднее время ответа
avg_over_time({service="app-dev"} | json | unwrap duration [5m])

# Максимальное время
max_over_time({service="app-dev"} | json | unwrap duration [5m])

# Сумма
sum_over_time({service="app-dev"} | json | unwrap bytes [5m])
```

### Форматирование вывода

```logql
# Форматирование строки
{service="app-dev"} | json | line_format "{{.timestamp}} [{{.level}}] {{.message}}"

# Только определенные поля
{service="app-dev"} | json | line_format "{{.userId}}: {{.message}}"
```

---

## 📊 Типы визуализаций

| Тип | Когда использовать | Пример |
|-----|-------------------|--------|
| **Time series** | Графики во времени | RPS, ошибки, время ответа |
| **Stat** | Одно большое число | Количество ошибок, активных пользователей |
| **Gauge** | Процент или прогресс | Success rate, CPU usage |
| **Bar chart** | Сравнение категорий | Ошибки по сервисам |
| **Pie chart** | Распределение | Уровни логов, типы запросов |
| **Table** | Табличные данные | Топ ошибок, список пользователей |
| **Logs** | Просмотр логов | Последние ошибки, запросы |
| **Heatmap** | Распределение значений | Время ответа по времени суток |

---

## 🎨 Настройки панелей

### Цвета и пороги

```
Thresholds (Пороги):
- Green: 0-10 (хорошо)
- Yellow: 10-50 (предупреждение)
- Red: >50 (критично)

Color mode:
- Value: цвет текста
- Background: цвет фона
```

### Единицы измерения

```
Time:
- ms (milliseconds)
- s (seconds)
- m (minutes)

Data:
- bytes
- kbytes
- mbytes

Throughput:
- reqps (requests per second)
- ops (operations per second)

Misc:
- percent (0-100)
- short (1.2k, 1.5M)
```

### Легенда

```
Legend mode:
- List: список
- Table: таблица
- Hidden: скрыть

Legend values:
- Min, Max, Mean
- Last, Total
- Current
```

---

## 🔧 Переменные

### Создание переменной

```
Dashboard settings → Variables → New

Name: service
Type: Query
Query: label_values(service)
Data source: Loki
```

### Использование

```logql
# В запросах
{service="$service"}

# Regex
{service=~"$service"}

# Множественный выбор
{service=~"$service:regex"}
```

### Типы переменных

- **Query** — из запроса к источнику данных
- **Custom** — список значений вручную
- **Text box** — ввод текста
- **Constant** — константа
- **Interval** — временной интервал

---

## ⚡ Горячие клавиши

| Клавиша | Действие |
|---------|----------|
| `e` | Explore |
| `d` | Dashboards |
| `s` | Search |
| `Ctrl+S` | Сохранить дашборд |
| `Ctrl+H` | Скрыть/показать легенду |
| `Ctrl+Z` | Отменить |
| `t` | Zoom out time |
| `Shift+T` | Zoom in time |

---

## 🚨 Алерты

### Создание алерта

```
1. Создайте панель с запросом
2. Alert → Create alert rule
3. Условие: WHEN last() OF query(A) IS ABOVE 10
4. Evaluate every: 1m
5. For: 5m (алерт сработает после 5 минут проблемы)
6. Notifications: выберите канал
```

### Каналы уведомлений

- **Email**
- **Slack**
- **Telegram**
- **Webhook**
- **PagerDuty**

---

## 📝 Полезные запросы для вашего проекта

### Мониторинг сервисов

```logql
# Все логи микросервисов
{service=~"(gateway-app-dev|app-dev|product-app-dev|order-app-dev|cart-app-dev)"}

# Статус сервисов (работают ли)
count(count_over_time({service=~".*-app-dev"} [1m])) by (service)

# RPS по сервисам
sum(rate({service=~".*-app-dev"} [1m])) by (service)
```

### Ошибки

```logql
# Все ошибки
{service=~".*-app-dev"} |~ "(?i)error|exception|failed"

# Ошибки по сервисам
sum(count_over_time({service=~".*-app-dev"} |~ "(?i)error" [5m])) by (service)

# Error rate (процент ошибок)
sum(count_over_time({service=~".*-app-dev"} |~ "error" [5m])) / sum(count_over_time({service=~".*-app-dev"} [5m])) * 100
```

### Kafka

```logql
# Отправленные сообщения
{service=~".*-app-dev"} |= "Kafka" |= "published"

# Полученные сообщения
{service=~".*-app-dev"} |= "Kafka" |= "consumed"

# Ошибки Kafka
{service=~".*-app-dev"} |= "Kafka" |~ "(?i)error"
```

### gRPC

```logql
# Все gRPC вызовы
{service=~".*-app-dev"} |= "gRPC"

# gRPC ошибки
{service="cart-app-dev"} |= "gRPC" |~ "(?i)error|failed"

# gRPC от Cart к Product
{service="cart-app-dev"} |= "gRPC" |= "product"
```

### База данных

```logql
# Все запросы к БД
{service=~".*-app-dev"} |~ "query|SELECT|INSERT|UPDATE|DELETE"

# Медленные запросы (если логируете duration)
{service=~".*-app-dev"} | json | duration > 1000

# Ошибки БД
{service=~".*-app-dev"} |~ "(?i)database|postgres" |~ "error"
```

### Бизнес-метрики

```logql
# Регистрации пользователей
{service="app-dev"} |= "User created"

# Созданные заказы
{service="order-app-dev"} |= "Order created"

# Добавления в корзину
{service="cart-app-dev"} |= "Product added to cart"
```

### Trace ID

```logql
# Все логи одного запроса
{service=~".*-app-dev"} |= "traceId=abc-123-def"

# Или с JSON
{service=~".*-app-dev"} | json | traceId="abc-123-def"
```

---

## 🎯 Best Practices

### ✅ DO

- Используйте метки для фильтрации (service, level)
- Логируйте в JSON формате
- Добавляйте traceId для отслеживания запросов
- Создавайте дашборды для каждого сервиса
- Настраивайте алерты на критичные метрики
- Используйте переменные для гибкости дашбордов

### ❌ DON'T

- Не добавляйте userId как метку (высокая cardinality)
- Не логируйте пароли и токены
- Не создавайте слишком сложные запросы (медленно)
- Не храните логи вечно (настройте retention)
- Не игнорируйте ошибки в логах

---

## 🔗 Полезные ссылки

- [Grafana Docs](https://grafana.com/docs/grafana/latest/)
- [LogQL Syntax](https://grafana.com/docs/loki/latest/logql/)
- [Dashboard Examples](https://grafana.com/grafana/dashboards/)
- [Alerting Guide](https://grafana.com/docs/grafana/latest/alerting/)

---

## 💡 Советы

### 1. Начните с Explore
Сначала тестируйте запросы в Explore, потом добавляйте на дашборд.

### 2. Используйте временные диапазоны
- Last 5 minutes — для дебага
- Last 1 hour — для мониторинга
- Last 24 hours — для анализа

### 3. Сохраняйте дашборды в Git
Экспортируйте JSON и храните в репозитории.

### 4. Создайте плейлист
Для отображения на большом экране в офисе.

### 5. Документируйте запросы
Добавляйте описания к панелям (Panel → Description).

---

## 🎓 Практика

### Задание 1: Найдите ошибку
```logql
{service="cart-app-dev"} |~ "(?i)error" | json
```

### Задание 2: Создайте дашборд
- Панель 1: RPS всех сервисов
- Панель 2: Ошибки по сервисам
- Панель 3: Последние 10 ошибок

### Задание 3: Настройте алерт
Условие: Ошибок > 10 за 5 минут

### Задание 4: Отследите запрос
Найдите traceId и проследите запрос через все сервисы.

---

**Успехов в работе с Grafana! 🚀**
