PORT=5000
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=users-service
PRIVAT_KEY=secret_key_safasf

MONGO_URI=localhost:27017
MONGO_DB=logs


REDIS_HOST=localhost
REDIS_PORT=6380
REDIS_PASSWORD=321321

# Kafka broker address for apps running on your PC (host)
# We expose Kafka on localhost:9094 in docker-compose.dev.yml
KAFKA_BROKERS=localhost:9094

# Used as Kafka clientId (helps identify producer/consumer in logs/metrics)
KAFKA_CLIENT_ID=user-service

# Consumer group is required by Nest Kafka transport even if you only produce
KAFKA_GROUP_ID=user-service

